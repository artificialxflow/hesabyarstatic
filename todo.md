# چک‌لیست پروژه حساب‌یار (EasyBooks Iran / Hesabyar)

نرم‌افزار جامع حسابداری کسب‌وکارهای کوچک و اصناف — نسخه وب استاتیک و پنل دسکتاپ (HTML / CSS / JavaScript / Tailwind CSS)  
**محدوده:** فقط فرانت‌اند استاتیک؛ بدون بک‌اند، سرور و API واقعی. داده‌ها نمونه (Mock) در جاوااسکریپت.

---

## ۰. اصول ثابت طراحی (در تمام مراحل اعمال شود)

- [x] تمام صفحات `dir="rtl"` و `lang="fa"` پیش‌فرض داشته باشند.
- [x] تایپوگرافی فارسی (مثلاً وزیرمتن / IRANSans / مشابه وب‌فونت مجاز) در بدنه و عناوین اعمال شود.
- [x] تم کلی روشن (Light) باشد؛ کارت‌ها، دکمه‌ها، جداول و فرم‌ها کادر پررنگ، حاشیه واضح و سایه ملایم داشته باشند.
- [x] رنگ‌ها برای بخش‌های حسابداری تفکیک شوند (فاکتور، اسناد، انبار، گزارش، هشدار معوقه).
- [x] آیکون‌های مدرن آنلاین + ایموجی کاربردی در عناوین کلیدی استفاده شود.
- [x] طراحی کاملاً واکنش‌گرا (موبایل، تبلت، دسکتاپ عریض) باشد.
- [x] دکمه تغییر زبان FA / EN در هدر لندینگ و هدر پنل کار کند و متن‌ها، تاریخ و جهت صفحه را هماهنگ کند.
- [x] هیچ وابستگی به سرور واقعی نباشد؛ همه فرم‌ها و فیلترها در کلاینت کار کنند.

---

## ۱. راه‌اندازی پروژه و زیرساخت فرانت

- [x] ساختار پوشه‌ها را مشخص کن: `index.html` (لندینگ)، `app/` یا `dashboard.html` (پنل)، `assets/css`، `assets/js`، `assets/img`، `assets/i18n`.
- [x] Tailwind CSS را به روش استاتیک (CDN یا فایل بیلد شده) به پروژه وصل کن.
- [x] فایل CSS سفارشی برای RTL، فونت، بوردرهای حسابداری و وضعیت‌های رنگی بنویس.
- [x] پیکربندی `dir` و کلاس‌های منطقی Tailwind (`ms-` / `me-` / `start` / `end`) را برای RTL/LTR آماده کن.
- [x] فایل ترجمه دوزبانه (`fa.json` / `en.json` یا آبجکت JS معادل) با کلیدهای مشترک لندینگ و پنل بساز.
- [x] ماژول کوچک `i18n.js` برای تعویض زبان، ذخیره انتخاب در `localStorage` و به‌روزرسانی `dir`/`lang` بنویس.
- [x] پالت رنگ، شعاع گوشه، ضخامت بوردر و سایه‌ها را در یک منبع واحد (CSS variables) تعریف کن.
- [x] مجموعه داده نمونه (مشتریان، کالاها، فاکتورها، اسناد دوبل، موجودی انبار، گزارش‌ها) را در `mock-data.js` آماده کن.

---

## ۲. سیستم طراحی رابط کاربری (UI Kit)

- [x] هدر، لوگو متنی «حساب‌یار / ایزی‌کتاب» و دکمه CTA را طراحی کن.
- [x] دکمه‌های اصلی، ثانویه، خطر و موفقیت با کادر مشخص بساز.
- [x] کارت آماری رنگی (موجودی، درآمد، هزینه، سود) با آیکون و عدد بزرگ بساز.
- [x] جدول حرفه‌ای با هدر ثابت، ردیف‌های جدا، وضعیت رنگی و اسکرول افقی در موبایل بساز.
- [x] بج وضعیت: پرداخت‌شده، معوقه، پیش‌نویس، باطل‌شده.
- [x] فیلد فرم، سلکت سرفصل حساب، فیلتر تاریخ و جستجو را یکدست کن.
- [x] مودال / دراور ثبت سند و صدور فاکتور را با فوکوس و بستن با Escape طراحی کن.
- [x] سایدبار دسکتاپ + منوی همبرگری موبایل پنل را طراحی کن.
- [x] حالت خالی جدول و حالت بدون نتیجه جستجو را مشخص کن.
- [x] اطمینان حاصل کن همه کنترل‌ها در LTR انگلیسی هم تراز و خوانا هستند.

---

## ۳. لندینگ پیج معرفی (وب + اندروید)

### ۳.۱ هدر و ناوبری

- [x] لوگو، منوی: ویژگی‌ها، نظرات، سوالات متداول، ورود به پنل.
- [x] دکمه تغییر زبان FA/EN در هدر.
- [x] دکمه «دانلود نسخه اندروید» (لینک فایل APK ساختگی یا بخش دانلود).
- [x] دکمه «ورود به حساب کاربری» که به پنل استاتیک هدایت کند.
- [x] هدر چسبان با کادر پایین مشخص؛ منوی موبایل جمع‌شونده.

### ۳.۲ بخش اصلی (Hero)

- [x] شعار فارسی درباره حسابداری آسان، فاکتوردهی سریع و گزارش دقیق برای اصناف ایرانی.
- [x] زیرعنوان کوتاه + دو CTA: ورود به پنل و دانلود اندروید.
- [x] نمای شبیه‌ساز موبایل (Mockup) اپ اندروید در کنار متن Hero.
- [x] داخل Mockup چند صفحه نمونه (داشبورد کوچک، فاکتور، موجودی) به صورت استاتیک نمایش داده شود.
- [x] چیدمان Hero در موبایل ستونی و در دسکتاپ دو ستونه RTL باشد.

### ۳.۳ ویژگی‌های کلیدی (کارت‌های رنگی حاشیه‌دار)

- [x] کارت فاکتوردهی مشتریان: ساخت و ارسال فاکتور رسمی با قالب شیک.
- [x] کارت حسابداری کسب‌وکار: اسناد دوبل، حساب کل / معین / تفصیلی.
- [x] کارت گزارش‌های مالی: ترازنامه، دفتر روزنامه، سود و زیان.
- [x] کارت موجودی انبار: ردیابی آنی کالا و فروش.
- [x] هر کارت رنگ، آیکون و بوردر مستقل داشته باشد.

### ۳.۴ نظرات، FAQ و فوتر

- [x] بخش نظرات کاربران (۳ تا ۶ کارت نقل‌قول با نام صنف نمونه).
- [x] سوالات متداول آکاردئونی (حداقل ۵ سؤال: دوبل، فاکتور، انبار، گزارش، نسخه اندروید).
- [x] فوتر کامل: معرفی کوتاه، لینک‌ها، تماس فرضی، کپی‌رایت حساب‌یار، سوییچ زبان تکراری در فوتر در صورت نیاز.
- [x] انکر لینک‌های منو به بخش‌ها اسکرول نرم داشته باشند.

---

## ۴. پنل ادمین و داشبورد حسابداری (وب / دسکتاپ)

### ۴.۱ پوسته پنل (Shell)

- [x] لایه کلی: سایدبار + هدر داخلی + محتوای اصلی.
- [x] سایدبار: داشبورد، فاکتورها، اسناد حسابداری، انبارداری، گزارش‌ها، تنظیمات.
- [x] آیتم فعال منو مشخص و رنگی باشد.
- [x] هدر پنل: عنوان صفحه، جستجوی سراسری ظاهری، سوییچ زبان، نام کاربر نمونه.
- [x] در عرض کم، سایدبار به کشو تبدیل شود.
- [x] ظاهر «نرم‌افزار دسکتاپ»: تراکم اطلاعاتی بالاتر، جداول پهن، کادرهای مشخص.

### ۴.۲ داشبورد آماری

- [x] کارت موجودی کل (رنگ متمایز + عدد قالب‌بندی‌شده ریال).
- [x] کارت درآمد ماهانه.
- [x] کارت هزینه‌ها.
- [x] کارت سود خالص.
- [x] نمودار ساده بصری (Canvas یا SVG استاتیک/JS) برای روند درآمد/هزینه.
- [x] ویجت آخرین فاکتورها و آخرین اسناد با وضعیت رنگی.
- [x] اعداد و تاریخ در حالت EN به فرمت لاتین و در FA به فارسی تبدیل شوند.

### ۴.۳ فاکتورها (صدور و فهرست)

- [x] جدول فاکتورها: شماره، مشتری، تاریخ، مبلغ، وضعیت، عملیات.
- [x] فیلتر وضعیت (پرداخت‌شده / معوقه / همه) و جستجوی نام مشتری.
- [x] دکمه «فاکتور جدید» فرم تعاملی را باز کند.
- [x] فرم فاکتور: مشتری، اقلام کالا، تعداد، قیمت، جمع جزء، مالیات ظاهری، جمع کل.
- [x] افزودن/حذف ردیف کالا در فرم بدون رفرش صفحه.
- [x] پیش‌نمایش قالب فاکتور شیک (چاپ‌گونه) در پنل یا مودال.
- [x] ذخیره فقط در حافظه مرورگر / آرایه JS (بدون سرور).

### ۴.۴ اسناد حسابداری دوبل

- [x] جدول اسناد اخیر: شماره سند، تاریخ، شرح، جمع بدهکار، جمع بستانکار، وضعیت تراز.
- [x] جستجو و فیلتر بازه تاریخ در کلاینت.
- [x] فرم ثبت سند جدید: شرح، تاریخ، ردیف‌های بدهکار/بستانکار.
- [x] انتخاب سرفصل: کل، معین، تفصیلی (لیست نمونه درختی یا سلکت وابسته).
- [x] اعتبارسنجی ظاهری: جمع بدهکار = جمع بستانکار قبل از «ثبت».
- [x] نمایش خطای عدم تراز با کادر هشدار پررنگ.
- [x] سند ثبت‌شده به جدول بالا اضافه شود (state محلی).

### ۴.۵ انبارداری

- [x] جدول کالا: کد، نام، واحد، موجودی، نقطه سفارش، ارزش تقریبی.
- [x] نشان رنگی موجودی کم / کافی.
- [x] فرم کالای جدید و ویرایش موجودی نمونه.
- [x] نمایش اثر فروش فاکتور روی موجودی به‌صورت شبیه‌سازی‌شده (کاهش عدد در داده محلی).

### ۴.۶ گزارش‌های مالی

- [x] صفحه گزارش با تب یا کارت: ترازنامه، دفتر روزنامه، سود و زیان.
- [x] جداول نمونه با ارقام Mock و جمع ردیف.
- [x] فیلتر ماه / سال ظاهری که داده نمونه را عوض کند.
- [x] دکمه چاپ / خروجی ظاهری (window.print با استایل چاپ جداول).

### ۴.۷ تنظیمات

- [x] نام کسب‌وکار، واحد پول (ریال / تومان نمایشی)، لوگو متنی.
- [x] انتخاب زبان پیش‌فرض و ذخیره در localStorage.
- [x] تنظیم نمایش تم روشن (ثابت طبق الزام پروژه) و شدت کادرها در صورت تمایل فقط CSS.
- [x] متن راهنمای «نسخه استاتیک؛ داده واقعی ذخیره سروری ندارد».

---

## ۵. منطق کلاینت، داده نمونه و تعاملات

- [x] قالب‌بندی عدد فارسی/انگلیسی و جداکننده هزارگان.
- [x] تاریخ شمسی نمایشی در FA و میلادی در EN (حتی اگر تبدیل کامل تقویم ساده باشد، یکدست باشد).
- [x] فیلتر، جستجو و مرتب‌سازی جداول فقط با JS.
- [x] اعتبارسنجی فرم‌ها (فیلد اجباری، مبلغ نامعتبر، عدم تراز سند).
- [x] پیام موفقیت ثبت فاکتور/سند با اعلان غیرمزاحم.
- [x] جلوگیری از ارسال واقعی فرم (`preventDefault`) در همه جا.
- [x] داده نمونه کافی برای پر بودن داشبورد، جداول و نمودار در اولین ورود.

---

## ۶. واکنش‌گرایی، دسترس‌پذیری و کیفیت

- [x] لندینگ در عرض ۳۶۰px، ۷۶۸px و ۱۲۸۰px بررسی شود.
- [x] پنل در لپ‌تاپ و دسکتاپ عریض خوانا باشد؛ جداول در موبایل اسکرول افقی داشته باشند.
- [x] Mockup اندروید در Hero در موبایل زیر متن و با اندازه مناسب باشد.
- [x] فوکوس کیبورد روی دکمه‌ها، منو و مودال مشخص باشد.
- [x] کنتراست متن روی کارت‌های رنگی کافی باشد.
- [x] تصاویر و آیکون‌ها `alt` فارسی/انگلیسی متناسب با زبان فعال داشته باشند.
- [x] فونت و جهت پس از تعویض زبان بدون به‌هم‌ریختگی جدول و سایدبار اعمال شود.

---

## ۷. یکپارچگی لندینگ و پنل

- [x] لینک «ورود» از لندینگ به پنل و «خروج / بازگشت به سایت» از پنل به لندینگ.
- [x] زبان انتخاب‌شده بین دو بخش حفظ شود.
- [x] هویت بصری (رنگ، لوگو، بوردر، فونت) در هر دو بخش یکسان باشد.
- [x] عنوان تب مرورگر و متا توضیحات فارسی برای لندینگ تنظیم شود.

---

## ۸. تحویل و کنترل نهایی

- [x] همه صفحات بدون خطای کنسول در مرورگر باز شوند.
- [x] هیچ درخواست بک‌اند شکست‌خورده در Network نباشد (فقط CDN فونت/آیکون/Tailwind در صورت استفاده).
- [x] چک‌لیست بصری: تم روشن، کادر پررنگ، RTL فارسی، سوییچ EN.
- [x] مسیرهای اصلی کاربر را دستی طی کن: لندینگ → ویژگی‌ها → ورود → داشبورد → فاکتور جدید → سند دوبل → انبار → گزارش.
- [x] فایل‌های نهایی را طوری بچین که با باز کردن `index.html` کل دمو قابل ارائه باشد.

---

## ترتیب پیشنهادی اجرا

1. زیرساخت، Tailwind، توکن رنگ، i18n و داده Mock  
2. UI Kit و پوسته پنل  
3. لندینگ کامل شامل Mockup اندروید  
4. داشبورد و جداول فاکتور/سند/انبار/گزارش  
5. فرم‌های تعاملی و اعتبارسنجی دوبل  
6. پرداخت واکنش‌گرا، زبان و کنترل نهایی تحویل
